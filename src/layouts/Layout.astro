---
interface Props {
  title: string;
  description: string;
}
const { title, description } = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;0,9..144,700;1,9..144,600&family=Nunito:wght@400;700;800;900&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
  </head>
  <body>
    <nav>
      <div class="nav-inner">
        <a href="/" class="logo" aria-label="SecureLayer home">
          <img src="/logo.svg" alt="" class="logo-img" />
          <span class="logo-text">Secure<span>Layer</span></span>
        </a>
        <div class="nav-links">
          <a href="/free-cybersecurity-ai-prompts">Free Prompts</a>
          <a href="/github-security-assessment">GitHub Audit</a>
          <a href="/#start" class="btn-nav">Get Started</a>
        </div>
      </div>
    </nav>
    <slot />
    <footer>
      <div class="footer-inner">
        <p>Choose cybersecurity, it's cheaper.</p>
        <p class="footer-links">
          <a href="/security">Security</a>
          <a href="/sustainability">Environment</a>
        </p>
        <a href="/sustainability" class="footer-green-link">
          <img src="/gwf-badge.png" alt="This website runs on green hosting - verified by thegreenwebfoundation.org" width="200" height="95" />
        </a>
        <p class="footer-copy">&copy; 2026 SecureLayer. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  *, *:before, *:after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --bg:        #f0e9da;
    --bg2:       #e8dfc9;
    --bg3:       #e0d5b9;
    --border:    rgba(33,29,22,.13);
    --border2:   rgba(0,0,0,.06);
    --cyan:      #e0a32e;
    --cyan-dim:  rgba(224,163,46,.14);
    --green:     #2f9e5c;
    --purple:    #2f6b4a;
    --text:      #2b2619;
    --muted:     #7d7460;
    --white:     #17140f;
    --radius:    24px;
    --font:      "Inter", sans-serif;
    --font-head: "Fraunces", serif;
    --font-round:"Nunito", sans-serif;
    --font-mono: "JetBrains Mono", "Courier New", monospace;
  }

  html { scroll-behavior: smooth; }
  body { background: var(--bg); color: var(--text); font-family: var(--font); line-height: 1.6; -webkit-font-smoothing: antialiased; }

  /* NAV */
  nav { position: fixed; top: 0; left: 0; right: 0; z-index: 100; background: rgba(240,233,218,.88); backdrop-filter: blur(12px); border-bottom: 1px solid var(--border); }
  .nav-inner { max-width: 1100px; margin: 0 auto; padding: 0 24px; height: 64px; display: flex; align-items: center; justify-content: space-between; }
  .logo { display: inline-flex; align-items: center; gap: 10px; text-decoration: none; }
  .logo-img { height: 32px; width: 32px; filter: brightness(0); flex-shrink: 0; }
  .logo-text { font-family: var(--font-round); font-size: 1.25rem; font-weight: 800; color: var(--white); }
  .logo-text span { color: var(--cyan); }
  .nav-links { display: flex; align-items: center; gap: 28px; }
  .nav-links a:not(.btn-nav) { color: var(--muted); text-decoration: none; font-size: .875rem; transition: color .2s; }
  .nav-links a:not(.btn-nav):hover, .nav-links a.active { color: var(--text); }
  .btn-nav { background: var(--cyan); border: 1px solid var(--cyan); color: #17140f; padding: 8px 20px; border-radius: 100px; font-family: var(--font-round); font-size: .875rem; font-weight: 800; text-decoration: none; transition: opacity .2s; }
  .btn-nav:hover { opacity: .85; }

  /* SHARED SECTION ELEMENTS */
  .section-label { font-size: .7rem; font-weight: 600; letter-spacing: .12em; text-transform: uppercase; color: var(--cyan); margin-bottom: 16px; }
  h2 { font-family: var(--font-head); font-size: clamp(1.5rem, 3.5vw, 2.2rem); font-weight: 700; color: var(--white); margin-bottom: 16px; }
  .section-sub { color: var(--muted); max-width: 560px; margin: 0 auto 60px; }

  /* PROMPT CARD (used by free-prompts page) */
  .prompt-card { background: var(--bg2); border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; }
  .prompt-header { display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 12px; padding: 16px 20px; border-bottom: 1px solid var(--border); background: var(--bg3); }
  .prompt-meta { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
  .badge-tier { font-size: .7rem; font-weight: 700; letter-spacing: .06em; text-transform: uppercase; padding: 3px 10px; border-radius: 100px; }
  .badge-tier.free { background: rgba(34,197,94,.12); color: var(--green); border: 1px solid rgba(34,197,94,.2); }
  .badge-tested { display: inline-flex; align-items: center; gap: 4px; font-size: .72rem; color: var(--green); font-weight: 500; }
  .badge-tokens { font-size: .72rem; color: var(--muted); background: var(--bg); border: 1px solid var(--border2); padding: 2px 8px; border-radius: 6px; }
  .badge-cat { font-size: .72rem; color: var(--cyan); background: var(--cyan-dim); border: 1px solid var(--border); padding: 2px 8px; border-radius: 6px; }
  .badge-expertise { font-size: .72rem; color: var(--purple); background: rgba(47,107,74,.08); border: 1px solid rgba(47,107,74,.15); padding: 2px 8px; border-radius: 6px; }
  .btn-copy { display: inline-flex; align-items: center; gap: 6px; background: var(--cyan-dim); border: 1px solid rgba(224,163,46,.3); color: var(--cyan); font-size: .8rem; font-weight: 500; padding: 7px 14px; border-radius: 8px; cursor: pointer; transition: background .2s; font-family: var(--font); white-space: nowrap; }
  .btn-copy:hover { background: rgba(224,163,46,.22); }
  .prompt-title { font-family: var(--font-head); font-size: 1.05rem; color: var(--white); padding: 20px 20px 12px; }
  .prompt-tags { display: flex; gap: 6px; flex-wrap: wrap; padding: 0 20px 16px; }
  .tag { font-size: .68rem; color: var(--muted); background: var(--bg3); border: 1px solid var(--border2); padding: 2px 8px; border-radius: 4px; }
  .prompt-body { font-family: var(--font-mono); font-size: .8rem; line-height: 1.7; color: #cbd5e1; background: #0a0f1a; padding: 20px; overflow-x: auto; white-space: pre-wrap; word-break: break-word; border-top: 1px solid var(--border); }
  .prompt-tested-note { font-size: .75rem; color: var(--muted); padding: 12px 20px; border-top: 1px solid var(--border); background: var(--bg3); }

  /* FOOTER */
  footer { border-top: 1px solid var(--border); padding: 48px 24px; text-align: center; }
  .footer-inner { max-width: 1100px; margin: 0 auto; }
  .footer-logo { display: inline-block; margin-bottom: 8px; opacity: .8; transition: opacity .2s; }
  .footer-logo:hover { opacity: 1; }
  .footer-logo img { display: block; }
  .footer-inner .logo { display: inline-block; margin-bottom: 12px; }
  .footer-inner p { color: var(--muted); font-size: .875rem; }
  .footer-links { margin-top: 16px; display: flex; justify-content: center; gap: 24px; }
  .footer-links a { font-size: .8rem; color: var(--muted); text-decoration: none; }
  .footer-links a:hover { color: var(--cyan); }
  .footer-copy { margin-top: 12px; font-size: .75rem; }
  .footer-green-link { display: inline-block; margin-top: 20px; opacity: .85; transition: opacity .2s; }
  .footer-green-link:hover { opacity: 1; }

  @media (max-width: 640px) {
    .nav-links a:not(.btn-nav) { display: none; }
    .prompt-header { flex-direction: column; align-items: flex-start; }
  }
</style>
